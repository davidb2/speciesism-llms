Code for the paper "Why AI systems don't like animals: uncovering speciesist biases in language models"


## Developer instructions

Use `pipreqs . --force` to update the `requirements.txt` file (`pip install pipreqs`).